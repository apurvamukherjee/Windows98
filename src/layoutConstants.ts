/**
 * Must stay in sync with --win98-taskbar-height in src/styles/global.css —
 * layout math here (snap/maximize/fullscreen rects) needs it as a number,
 * not a CSS custom property.
 */
export const TASKBAR_HEIGHT_PX = 28;
